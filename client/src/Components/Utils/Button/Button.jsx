import "./Button.css"

export const Button = () => {
    return (
      <main className="containerButton">
        <section className="relative h-screen w-[30%] bg-black">

        </section>

      </main>
    );
  };